/*
 * The newest Vesktop release and default-branch commit a maintainer has reviewed.
 *
 * These are review cursors, not necessarily the code currently bundled by DockView.
 * Advance them after deciding to adopt, defer, or reject an upstream change so the
 * daily watcher reports only changes that have not received a human decision.
 */
export const VESKTOP_REVIEWED_VERSION = "1.6.5";
export const VESKTOP_REVIEWED_COMMIT = "f054ca2f0312e31d8d620bb5f5b1766d9e6ee4f0";
