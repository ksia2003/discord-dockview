/*
 * The stable Vencord release compiled together with DockView.
 *
 * Keep this as a plain exported string literal: the scheduled upstream-maintenance
 * workflow updates this one file when it prepares a Vencord candidate PR. The
 * full commit protects builds from a stable tag being moved after detection.
 */
export const VENCORD_REF = "v1.14.16";
export const VENCORD_COMMIT = "fc5466ebf854a82c8385c41742162c4b66f8a119";
